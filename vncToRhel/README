Run this in Mac

```
./build-client.sh
source ~/.bash_profile
myVnctunnel $RHEL_REMOTE_HOST

# Then use vncviewer in localhost:5911
```

Run this in RHEL

```
sudo yum -y install git && \
git clone http://github.com/boonjiashen/remoteEc2 && \
cd remoteEc2 && \
./build-remote.sh
```

To SSH from mac to RHEL

```
ssh -i $RHEL_PEM $RHEL_REMOTE_USER@$RHEL_REMOTE_HOST
```
