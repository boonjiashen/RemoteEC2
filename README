Run this in Mac

```
chmod +x build-client.sh
./build-client.sh
source ~/.bash_profile
```

Run this in RHEL

```
chmod +x build-remote.sh
./build-remote.sh
```

To SSH from mac to RHEL

```
ssh -i $RHEL_PEM $REMOTE_USER@$RHEL_REMOTE_HOST
```
